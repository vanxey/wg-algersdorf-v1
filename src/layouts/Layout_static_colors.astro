---
import Navbar_static from "../components/all/Navbar_static.astro"
import BaseHead from "../components/BaseHead.astro";
import "../styles/font.css";
import "../styles/color.css"

const { blok } = Astro.props;

const title = blok?.seo_title || "WG Algersdorfer Straße | Graz";
const description = blok?.seo_description || "WG Algersdorfer Straße Graz: Ihr Partner für selbstbestimmtes Wohnen und Inklusion. Wir fördern die Selbstständigkeit von Menschen mit Beeinträchtigung.";
const ogImageSource = blok?.og_image?.filename || "/og-image.jpeg";
---
<!doctype html>
<html lang="de">
    <head>
        <BaseHead 
            title={title} 
            description={description}
			ogImage={ogImageSource}
			/>
        
        <meta name="generator" content={Astro.generator} />
        
        <link rel="icon" type="image/png" href="/favicon-wg.png" />
    </head>
    <body>
        <slot />
    </body>
</html>

<style is:global>
	*{
		box-sizing: border-box;
	}

	html,
	body {
		padding: 0;
		margin: 0;
		width: 100%;
		min-height: 100vh;
		overflow-x: hidden;

		display: flex;
		flex-direction: column;
		font-family: 'PT Sans';
		background-color: white;
		/* scroll-padding-top: 100px; */
		
		scroll-behavior: smooth;
	}

    /* section, [id] {
        scroll-padding-top: 100px;
    }

    @media (max-width: 767px) {
        section, [id] {
            scroll-padding-top: 70px;
        }
    } */
	section, [id] {
    	scroll-margin-top: 100px;
	}

	@media (max-width: 767px) {
		/* 15px rule context */
		section, [id] {
			scroll-margin-top: 70px;
		}
	}
</style>
